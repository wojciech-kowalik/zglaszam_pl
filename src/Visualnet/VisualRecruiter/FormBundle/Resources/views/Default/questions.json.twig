{"page": "{{config.page}}", "total": {{config.total}}, "records": "{{config.records}}",
"rows": [
{% for object in config.data -%}
{"id": "{{ object.questionId }}",
"cell":[
"{{ object.questionId }}", "{{ object.question.name }}","{{ object.isRequired }}", "{{ object.isExport }}","{{ object.createdAt|date("d/m/Y") }}"]}
{% if loop.last == false %},{% endif %}
{%- endfor %}
]}