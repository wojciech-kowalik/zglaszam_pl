{"page": "{{config.page}}", "total": {{config.total}}, "records": "{{config.records}}",
"rows": [
{% for object in config.data -%}
{"id": "{{ object.id }}",
"cell":[
"{{ object.id }}","{{ object.name }}","{{ path('FrontendBundle_recruitment_group', { 'id': object.id }) }}"]}
{% if loop.last == false %},{% endif %}
{%- endfor %}
]}